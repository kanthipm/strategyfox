function Pull(){
 
   let bname = document.getElementById("brand").value;
   let bdomain = document.getElementById("domain").value;
   let pname = document.getElementById("product").value;
  let disc = document.getElementById("discount").value;
  let dd = 
document.getElementById("details").value;
  let text = 
  "Create an Instagram Marketing post in 200 words for" + bname + "for their product" + pname + "which has a discount of" + dd +”. Make sure to include their domain name” + bdomain +” in the post
  console.log(text);
}
